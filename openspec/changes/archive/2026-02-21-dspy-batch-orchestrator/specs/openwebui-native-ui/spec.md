## ADDED Requirements

### Requirement: Native OpenWebUI ID-Message Hooks
The final JSON payload generated by the Socratic Explainer MUST strictly adhere to the OpenWebUI ID-Message schema conventions to render distinct visual structures.

#### Scenario: Horizontal & Vertical Layout Rendering
- **WHEN** the backend pushes the verified LaTeX chapter to the frontend.
- **THEN** the JSON structures it using OpenWebUI message IDs grouping "Mathematical Truth blocks" vertically and "Step-by-step Socratic walkthroughs" horizontally adjacent to the proof arrays.
