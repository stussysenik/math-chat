## 1. Nim Batch Orchestrator Core

- [ ] 1.1 Scaffold the `nimble` project structure inside the `src/` directory.
- [ ] 1.2 Implement the `http` module in Nim to accept standard mathematical image JSON payloads without blocking natively.
- [ ] 1.3 Implement a basic external process spawner in Nim to coordinate the pre-existing Python/SymPy routines.

## 2. Advanced Numerical Provers (GNU Octave & Wolfram)

- [ ] 2.1 Write the Nim-Octave bridge to pipe recursive generating function strings (e.g. `$a_{12}=233$`) directly into background Octave processes.
- [ ] 2.2 Register a Wolfram Alpha `httpclient` worker inside Nim to query deep symbolic limits and generating function coefficients.
- [ ] 2.3 Refactor the Verifier payload to tag the exact system (SymPy, Octave, or Wolfram) that successfully proved the mathematical node.

## 3. Seminar Book Horizontal-Vertical Output

- [ ] 3.1 Strip all existing generic DaisyUI components from the Socratic Explainer prompt templates.
- [ ] 3.2 Instruct the Socratic Explainer to format output as static textbook JSON objects.
- [ ] 3.3 Create a prototype `Maf.js` static HTML template strictly using `sp.latex()` strings, enforcing left-to-right calculation flows and top-to-bottom chapter flows.
